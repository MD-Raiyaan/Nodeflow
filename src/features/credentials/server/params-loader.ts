import { createLoader } from "nuqs/server";
import { credentialsParams } from "../../executions/paramscutions/paramsdentials/params";

export const credentialsParamsLoader=createLoader(credentialsParams);

